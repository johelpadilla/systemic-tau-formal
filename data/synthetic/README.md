# Synthetic data

Generators live in notebooks and `python/core` tests:

- Coupled logistic maps (pre-chaos vs chaos)
- Lorenz projections (optional, notebook 01)

Commit small CSV fixtures here when needed for CI; large trajectories stay generated on the fly.
